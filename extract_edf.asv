function [t,y] = extract_edf(signum,data,info,fs)
    
    %len = height(data.(signum){1}
    j = 0; 
    for i = 1:height(data)
        t_temp = (0:info.NumSamples(signum)-1)/fs(signum);
        t = vertcat(t,t_temp); 
        y_temp = data.(signum){i};
        y = vert
    end 
end